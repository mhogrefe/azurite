# Small-input variant of az_nat_mul_vs_nat (meanBitLength ≈ 500).
#
# At asymptotic sizes Nat's GMP backend dominates; this variant zooms into
# the small-input regime where the cost of AzNat's pure-Lean implementation
# is more directly comparable.

benchmark = "az_nat_mul_vs_nat"
limit     = 10000
title     = "az_nat_mul_vs_nat (small): Nat (GMP) vs AzNat"
x_label   = "Input size (significant_bits of a + b)"

[params]
meanBitLength = 500
iters         = 200
